<div class="cards ">
    <% @bbqs.each do |bbq|  %>
      <div class="card border border-secondary-subtle" style="width: 16em;">
        <%= image_tag "bbq.png", class: "h-15" %>
      <div class="card-body p-3" >
        <h5 class="card-title"> <%= bbq.name %> </h5>
        <p class=""><%= bbq.description %></p>
        <p class="">£<%= bbq.price %></p>
        <div class="link d-flex justify-content-center align-items-center">
        <%= link_to 'View', bbq_path(bbq), class: 'btn btn-primary' %>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>

<!DOCTYPE html>
<html>
  <head>
    <title>SmokeNGo</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
  </head>

  <body>
    <%= render "/shared/navbar" %>
    <div class="body-container">
    <%= yield %>
    </div>
    <p class="notice mb-0"><%= notice %></p>
    <p class="alert mb-0"><%= alert %></p>
<%# render the footer partial "_footer.html.erb" file from "shared" folder  everywhere on the app but on landing page%>
    <% unless action_name == "home" %>
       <%= render 'shared/footer' %>
    <% end %>
  </body>
</html>
